/* 
  /* 
   Practical 9 – Program 1
   Name: Suajal pandey -10405
   Reverse an Array
*/

#include <stdio.h>

int main() {
    int n, i;
    printf("Enter array size: ");
    scanf("%d", &n);

    int a[n];

    printf("Enter %d elements: ", n);
    for(i = 0; i < n; i++)
        scanf("%d", &a[i]);

    printf("\nReversed Array: ");
    for(i = n - 1; i >= 0; i--)
        printf("%d ", a[i]);

    return 0;
}

/*
Sample Output:
Enter array size: 5
Enter 5 elements: 1 2 3 4 5

Reversed Array: 5 4 3 2 1
*/ – Program 1
   Name: Arya Singh-10287
   Reverse an Array
*/

#include <stdio.h>

int main() {
    int n, i;
    printf("Enter array size: ");
    scanf("%d", &n);

    int a[n];

    printf("Enter %d elements: ", n);
    for(i = 0; i < n; i++)
        scanf("%d", &a[i]);

    printf("\nReversed Array: ");
    for(i = n - 1; i >= 0; i--)
        printf("%d ", a[i]);

    return 0;
}

/*
Sample Output:
Enter array size: 5
Enter 5 elements: 1 2 3 4 5

Reversed Array: 5 4 3 2 1
*/
