/*
    Name: Sujal pandey-10405
    Practical 8 - Q1: Function to return sum of digits
*/

#include <stdio.h>

int sumOfDigits(int n) {
    int sum = 0;
    while (n > 0) {
        sum=sum+ n % 10;
        n =n/10;
    }
    return sum;
}

int main() {
    int n;
    printf("Enter number: ");
    scanf("%d", &n);

    printf("Sum of digits = %d", sumOfDigits(n));

    /*
    OUTPUT:
    Enter number: 4123
    Sum of digits = 10
    */

    return 0;
}
