/* 
   Practical 10 – Program 3
   Name: Sujal pandey - 10405
   Row-wise Sum of 2D Array
*/

#include <stdio.h>

int main() {
    int r, c, i, j;

    printf("Enter rows & cols: ");
    scanf("%d %d", &r, &c);

    int a[r][c];

    printf("Enter elements:\n");
    for(i = 0; i < r; i++)
        for(j = 0; j < c; j++)
            scanf("%d", &a[i][j]);

    printf("\nRow-wise Sum:\n");
    for(i = 0; i < r; i++) {
        int sum = 0;
        for(j = 0; j < c; j++)
            sum += a[i][j];

        printf("Row %d Sum = %d\n", i + 1, sum);
    }

    return 0;
}

/*
Sample Output:
Enter rows & cols: 2 3
1 2 3
4 5 6

Row 1 Sum = 6
Row 2 Sum = 15
*/
